{
  "name": "sample-response",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "jest": {
    "watchPathIgnorePatterns": [
      "<rootDir>/tests/mock"
    ],
    "verbose": false
  },
  "scripts": {
    "test": "npm run build && npm run json-server && jest",
    "build": "rimraf dist/ && babel ./src --out-dir dist/",
    "json-server": "json-server --watch db.json &"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^7.0.0",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-jest": "^23.6.0",
    "babel-preset-es2015": "^6.24.1",
    "jest": "^23.6.0",
    "json-server": "^0.14.0",
    "regenerator-runtime": "^0.12.1",
    "rimraf": "^2.6.2"
  }
}
